Backend Query Tools
# Postman
# BFF
# GraphQL
# Swagger
# node.js

# Postman: GET(SELECT), POST(SELECT, INSERT, UPDATE, DELETE) の中で、POSTをテストできるツール
# BFF: Backend For Frontendの略で、フロントエンド開発者が必要とするAPIを提供するサーバーを指す
# GraphQL: REST(get, set, create, insert, select, delete などのAPI関数名…）APIの代わりに使用できるクエリ言語で、クライアントが必要なデータをリクエストできる
# Swagger: APIドキュメントを自動生成してくれるツール（BFF）
